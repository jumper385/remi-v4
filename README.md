# Pocketbase x Sveltekit Docker Compose
This serves as a template project for a quick to implement full-stack project.

To start, you just need to run the following command:
`docker-compose up --build -d`
